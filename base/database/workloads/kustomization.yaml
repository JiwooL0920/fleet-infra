apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Database Workloads - Wave 4b (Parallel - Depends on Operators)
# Data layer services that are managed by operators
resources:
  # PostgreSQL cluster (managed by CNPG operator)
  - cloudnative-pg.yaml
  
  # Redis cache and storage
  - redis.yaml