---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: database-operators
  namespace: flux-system
spec:
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./database/base/operators
  prune: true
  wait: true
  timeout: 5m0s
  dependsOn:
    - name: infrastructure-config
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars