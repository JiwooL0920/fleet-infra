apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Infrastructure Monitoring - Wave 4a (Parallel - Depends on Core)
# Monitoring and GitOps management tools
resources:
  # Monitoring stack
  - kube-prometheus-stack.yaml
  
  # GitOps dashboard and management
  - weave-gitops.yaml