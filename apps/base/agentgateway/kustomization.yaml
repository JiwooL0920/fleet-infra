apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: Flux uses ./base and ./config as separate paths (see base/services/agentgateway*.yaml).
# This top-level kustomization is for local `kubectl kustomize` builds only.
resources:
  - ./base
  - ./config
