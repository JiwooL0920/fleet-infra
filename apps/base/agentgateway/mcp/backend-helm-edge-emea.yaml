---
# AgentgatewayBackend: per-cluster helm tool federation for edge-emea (PLACEHOLDER).
# Uncomment in mcp/kustomization.yaml when edge-emea cluster is onboarded.
#
# --- UNCOMMENT ENTIRE RESOURCE WHEN edge-emea IS ONBOARDED ---
#
# apiVersion: agentgateway.dev/v1alpha1
# kind: AgentgatewayBackend
# metadata:
#   name: helm-mcp-edge-emea
#   namespace: agentgateway-system
#   labels:
#     cluster: edge-emea
#     domain: helm
# spec:
#   mcp:
#     targets:
#       - name: helm-tools-edge-emea
#         static:
#           host: kagent-tools.kagent.svc.cluster.local  # Update for remote cluster
#           port: 8084
#           protocol: StreamableHTTP
