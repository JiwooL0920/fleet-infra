---
# AgentgatewayBackend: per-cluster helm tool federation for platform-amer (PLACEHOLDER).
# Uncomment in mcp/kustomization.yaml when platform-amer cluster is onboarded.
#
# --- UNCOMMENT ENTIRE RESOURCE WHEN platform-amer IS ONBOARDED ---
#
# apiVersion: agentgateway.dev/v1alpha1
# kind: AgentgatewayBackend
# metadata:
#   name: helm-mcp-platform-amer
#   namespace: agentgateway-system
#   labels:
#     cluster: platform-amer
#     domain: helm
# spec:
#   mcp:
#     targets:
#       - name: helm-tools-platform-amer
#         static:
#           host: kagent-tools.kagent.svc.cluster.local  # Update for remote cluster
#           port: 8084
#           protocol: StreamableHTTP
