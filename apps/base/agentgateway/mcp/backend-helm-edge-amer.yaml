---
# AgentgatewayBackend: per-cluster helm tool federation for edge-amer (PLACEHOLDER).
# Uncomment in mcp/kustomization.yaml when edge-amer cluster is onboarded.
#
# --- UNCOMMENT ENTIRE RESOURCE WHEN edge-amer IS ONBOARDED ---
#
# apiVersion: agentgateway.dev/v1alpha1
# kind: AgentgatewayBackend
# metadata:
#   name: helm-mcp-edge-amer
#   namespace: agentgateway-system
#   labels:
#     cluster: edge-amer
#     domain: helm
# spec:
#   mcp:
#     targets:
#       - name: helm-tools-edge-amer
#         static:
#           host: kagent-tools.kagent.svc.cluster.local  # Update for remote cluster
#           port: 8084
#           protocol: StreamableHTTP
