apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Crossplane Providers and Configuration
# Deployed after Crossplane core is ready
resources:
  # AWS Providers only (S3, IAM, EC2)
  # This creates the provider CRDs that ProviderConfig depends on
  - ../crossplane/provider-aws
