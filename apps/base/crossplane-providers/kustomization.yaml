apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Crossplane Providers and Configuration
# Deployed after Crossplane core is ready
resources:
  # AWS Providers (S3, IAM, EC2)
  - ../crossplane/provider-aws
  
  # Provider Configuration and test resources
  - ../crossplane/crossplane-config