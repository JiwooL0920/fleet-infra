apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
   - ./base
   - ./agents
   # - ./alertmanager-hook    # Disabled until Docker images are built and loaded into cluster
   # - ./stream-dispatcher    # Disabled until Docker images are built and loaded into cluster
   - ./models
   - ./mcpservers
