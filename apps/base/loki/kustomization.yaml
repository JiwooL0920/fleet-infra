apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No namespace field here - HelmRelease defines its own namespace (flux-system)
# and targetNamespace (monitoring) for the actual workloads
resources:
  - helmrelease.yaml