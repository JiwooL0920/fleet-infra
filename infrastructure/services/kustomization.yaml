apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Infrastructure services - parallel deployment services
resources:
  # Foundation networking and ingress
  - traefik
  # Monitoring stack
  - kube-prometheus-stack
  # GitOps management dashboard
  - weave-gitops