apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Infrastructure configuration - depends on base services being ready
resources:
  # External secrets configuration (needs LocalStack + ESO running)
  - external-secrets-config