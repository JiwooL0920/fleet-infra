apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Infrastructure services - foundation layer
resources:
  # Foundation networking and ingress
  - traefik
  # Local AWS services emulation
  - localstack
  # Secrets management
  - external-secrets-operator
  # Monitoring stack
  - kube-prometheus-stack
  # GitOps management dashboard
  - weave-gitops